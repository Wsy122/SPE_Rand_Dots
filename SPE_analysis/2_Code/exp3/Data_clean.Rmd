---
title: "Data Claening"
output: html_notebook
---

```{r}
rm(list = ls())
```

**加载所需要的包**
```{r}
# 配置R包
if (!require(pacman)){
        install.packages("pacman")
        library(pacman)
}

pacman::p_load(
  "tidyverse", "ggplot2", "cowplot", "bruceR","ggstatsplot", "papaja",
  "palmerpenguins", "dplyr", "patchwork", "RColorBrewer"
)

source("Functions/R_rainclouds.R")
```

## 1. 读取数据

**读取目录下的原始数据**

```{r}
# 设置目标文件夹的上级路径
parent_folder <- "../../3_Data/exp3/RawData/"

# 指定需要读取的子文件夹名称
# sub_folders <- c("motion", "color")

# 创建空列表存储所有文件路径
file_list <- c()

file_list <- list.files(
    path = parent_folder,
    pattern = "*exp3_subj.*\\.csv$", 
    full.names = TRUE
  )

# 创建一个空列表来存储所有数据
df_list_1 <- list()

# 逐个读取文件并存储（保留原文件名作为列表元素名称）
for (file in file_list) {
  # 获取文件名（不带扩展名）
  file_name <- tools::file_path_sans_ext(basename(file))  
  # 将数据存储在列表中
  tryCatch({
    df_list_1[[file_name]] <- read.csv(file)
  })
}

# 可选：查看读取的文件数量和名称
cat("共读取", length(df_list_1), "个文件")
print(names(df_list_1))
```
## 2. 数据清洗

```{r}
# 创建输出文件夹（如果不存在）
output_path <- "../../3_Data/exp3/CleanData"
if (!dir.exists(output_path)) {
  dir.create(output_path, recursive = TRUE)
}

# 逐篇处理每个数据框
for (file_name in names(df_list_1)) {
  data <- df_list_1[[file_name]]

  # 提取被试信息
  participant_info <- data[data$part == "survey", "response"]
  participant_info <- gsub("[{}\"]", "", participant_info) # 去掉多余的字符
  info_list <- strsplit(participant_info, ",")[[1]]

  # 创建新的变量
  participant_data <- data.frame(
    subj_idx = file_name,
    Sex = as.character(NA),
    Age = as.numeric(NA),
    Handedness = as.character(NA),
    Color_blindness = as.character(NA),
    stringsAsFactors = FALSE
  )
  
  # 先按照下划线分割字符串
  split_data <- strsplit(participant_data$subj_idx, "_")
  # 提取分割后的第3个元素（数字部分在第3个位置，可根据实际调整）并转换为数值
  participant_data$subj_idx <- as.numeric(lapply(split_data, function(x) {
    gsub("[^0-9]", "", x[[3]])  # 提取纯数字部分
  }))

  for (info in info_list) {
    key_value <- strsplit(info, ":")[[1]]
    key <- trimws(key_value[1])
    value <- trimws(key_value[2])
    if (key == "sex"){
      participant_data$Sex <- switch(
        value, "男" = 1, "女" = 2
      )
    }
    if (key == "age") participant_data$Age <- as.numeric(value)
    if (key == "hands") participant_data$Handedness <- value
    if (key == "color_blindness") participant_data$Color_blindness <- value
  }

  # 提取行为数据
  behavior_data <- data[
    data$part %in% c("motion_test","RDK_overlap","RDK_color","RDK_shape"),
    c(
      "part", "rt", "response", "correct", "coherent_direction","coherence", 
      "isMatch", "association", "difficulty"
    )
  ]
  
  # 分别提取正式试次
  official_trials <- do.call(
  rbind,
  lapply(unique(behavior_data$part), function(part) {
    part_data <- behavior_data[behavior_data$part == part, ]
    if (part %in% c("RDK_overlap", "RDK_color", "RDK_shape")) {
      return(part_data[-(1:24), ])  # 直接去掉前16行
    } else {
      return(NULL)
    }
  })
)

  # 合并被试信息和行为数据
  final_data <- cbind(participant_data, official_trials)

  # 输出到新的CSV文件
  output_file <- file.path(output_path, paste0(file_name, "_Clean.csv"))
  write.csv(final_data, output_file, row.names = FALSE)
}
```


**读取清洗后的数据**

```{r message=FALSE}
# 设置目标文件夹相对路径
folder_path <- "../../3_Data/exp3/CleanData"

# 列出所有含有"pilot_expt_"的csv文件
file_list <- list.files(
  path = folder_path,
  pattern = "*exp3_subj_.*\\.csv$", full.names = TRUE
)

# 创建一个空列表来存储数据
df_list_1 <- list()

# 逐个读取文件并存储
for (file in file_list) {
  file_name <- tools::file_path_sans_ext(basename(file)) # 获取文件名（不带扩展名）
  df_list_1[[file_name]] <- read.csv(file) # 将数据存储在列表中
}

# 清除不需要的变量
rm(participant_info, info_list, participant_data, split_data,
   behavior_data, official_trials, final_data, output_file, file, file_list,
   file_name, folder_path, info, key, key_value, value)

# 把 df_list_1 传送给 data
data <- do.call(rbind, df_list_1)
```

## 3. 被试人口学信息统计
```{r}
# 基于有效被试的人口学信息统计
df.basic <- data %>%
  dplyr::select(subj_idx, Age, Sex) %>%
  dplyr::distinct(subj_idx, .keep_all = TRUE) %>%  # 根据subject去重，保留完整行
  # 创建分组变量（1-70为运动组，71-141为颜色组）
  dplyr::mutate(
    subj_group = dplyr::case_when(
      subj_idx >= 1 & subj_idx <= 35 ~ "Group1 (motion)",
      subj_idx >= 36 & subj_idx <= 70 ~ "Group2 (color)",
    )
  ) %>%
  # 按创建的分组变量分组，再汇总统计
  # dplyr::group_by(subj_group) %>%
  dplyr::summarise(
    N = length(subj_idx),  # 每组总人数
    Nf = length(Sex[Sex == "2"]),  # 每组女性人数（Sex=2）
    Nm = length(Sex[Sex == "1"]),  # 每组男性人数（Sex=1）
    Age_mean = round(mean(Age, na.rm = TRUE), 2),  # 每组平均年龄
    Age_sd = round(sd(Age, na.rm = TRUE), 2),  # 每组年龄标准差
    .groups = "drop"  # 取消分组，返回普通数据框
  )

df.basic
```
## 4. 数据预处理

定义函数，用来计算平均反应时、正确率和自我-他人差异(SPE)

```{r}
# 计算平均正确率和反应时
compute_rt_acc <- function(data){
  data <- data %>%
    dplyr::filter(part %in% c("RDK_overlap","RDK_color","RDK_shape")) %>%
    dplyr::filter(rt != -1) %>%
    dplyr::group_by(subj_idx, part, difficulty, association) %>%
    dplyr::summarise(
      # rt = mean(rt, na.rm = TRUE),  # 所有有效 trials 的平均反应时
      rt = mean(rt[correct == 1], na.rm = TRUE),  # 仅正确 trials 的平均反应时
      acc = sum(correct == 1, na.rm = TRUE) / n(),  # 正确率
      .groups = "drop")
  
  return(data)
}

# 计算自我-他人差异，即SPE
compute_diff <- function(data, keepMatch=FALSE){
  # 决定分组变量
  group_vars <- if(keepMatch) {
    c("subj_idx", "part", "difficulty")
  } else {
    c("subj_idx", "part", "difficulty")
  }
  
  # 执行数据处理流程
  data_agg <- data %>%
    dplyr::filter(part %in% c("RDK_overlap","RDK_color","RDK_shape")) %>%
    dplyr::group_by_at(group_vars) %>%  # 使用 group_by_at 接受变量名向量
    dplyr::summarise(
      rt_other = mean(rt[association == "other"], na.rm = TRUE),
      rt_self = mean(rt[association == "self"], na.rm = TRUE),
      acc_other = mean(acc[association == "other"], na.rm = TRUE),
      acc_self = mean(acc[association == "self"], na.rm = TRUE),
      .groups = "drop"
    )
  
  # 计算差值
  data_result <- data_agg %>%
    dplyr::mutate(
      rt_diff_os = rt_other - rt_self, # 反应时越快越好，他人-自我 > 0 为自我优势
      acc_diff_so = acc_self - acc_other # 正确率越高越好，自我-他人 > 0 为自我优势
    )
  
  return(data_result)
}
```

**分别提取形状和颜色的数据**

```{r}
data <- data %>%
  #对反应时进行筛选
  dplyr::filter(rt >= 100 & rt <= 4000, !is.na(rt)) 
# Motion
data1 <- data %>%
  dplyr::filter(
    subj_idx %in% c( seq(1, 40))) %>%  # 被试编号 1-35 为运动组
  #对反应时进行筛选
  # dplyr::filter(rt >= 100 & rt <= 3000, !is.na(rt)) %>%
  # 先将correct转换为数值型（TRUE=1, FALSE=0）
  dplyr::mutate(correct = as.numeric(as.logical(correct))) %>%
  # 计算每个被试的正确率
  dplyr::group_by(subj_idx, part, difficulty) %>%
  dplyr::mutate(subj_acc = mean(correct, na.rm = TRUE)) %>%
  dplyr::ungroup()


data_shape <- data1 %>%
  dplyr::select(
    subj_idx, rt, correct, part, response, association, difficulty
  ) %>%
  # 先将correct转换为数值型（TRUE=1, FALSE=0）
  dplyr::mutate(correct = as.numeric(as.logical(correct))) %>%
  # 把rt转换为以秒为单位
  dplyr::mutate(rt = rt / 1000) %>%
  dplyr::mutate(group = "shape") 

# 输出到新的CSV文件
# output_file <- base::file.path(output_path, "data_motion.csv")
# utils::write.csv(data_motion, output_file, row.names = FALSE)


data_shape_rt_acc <- compute_rt_acc(data1) %>%
  dplyr::mutate(group = "shape") 


data_shape_diff <- compute_diff(data_shape_rt_acc) %>%
  #类型为character的转换为因子类型，便于后续分析
  dplyr::mutate(subj_idx = as.factor(subj_idx))

# Color
data2 <- data %>%
  dplyr::filter(
    subj_idx %in% c( seq(41, 80))) %>% # 后40为颜色
  #对反应时进行筛选
  # dplyr::filter(rt >= 200 & rt <= 3000, !is.na(rt)) %>%
  # 先将correct转换为数值型（TRUE=1, FALSE=0）
  dplyr::mutate(correct = as.numeric(as.logical(correct))) %>%
  # 计算每个被试的正确率
  dplyr::group_by(subj_idx, part, difficulty) %>%
  dplyr::mutate(subj_acc = mean(correct, na.rm = TRUE)) %>%
  dplyr::ungroup()

data_color <- data2 %>%
  dplyr::select(
    subj_idx, rt, correct, part, response, association, difficulty
  ) %>%
  dplyr::mutate(correct = as.numeric(as.logical(correct))) %>%
  dplyr::mutate(rt = rt / 1000) %>%
  dplyr::mutate(group = "color") 


data_color_rt_acc <- compute_rt_acc(data2) %>%
  dplyr::mutate(group = "color")


data_color_diff <- compute_diff(data_color_rt_acc) %>%
  #类型为character的转换为因子类型，便于后续分析
  dplyr::mutate(subj_idx = as.factor(subj_idx))

data_color_diff <- compute_diff(data_color_rt_acc) %>%
  #类型为character的转换为因子类型，便于后续分析
  dplyr::mutate(subj_idx = as.factor(subj_idx))
```


**合并保存数据**
```{r}
data_sbfa <- dplyr::bind_rows(data_color_rt_acc, data_shape_rt_acc) %>%
  dplyr::mutate(
    task_type = case_when(
      # group = color 时的规则
      group == "color" & part == "RDK_color" ~ "relevant",
      group == "color" & part == "RDK_motion" ~ "irrelevant",
      # group = motion 时的规则
      group == "shape" & part == "RDK_color" ~ "irrelevant",
      group == "shape" & part == "RDK_shape" ~ "relevant",
      # 通用重叠任务
      part == "RDK_overlap" ~ "overlap",
      TRUE ~ NA_character_
    )
  )

# 输出到新的CSV文件
output_file <- base::file.path(output_path, "data_sbfa.csv")
utils::write.csv(data_sbfa, output_file, row.names = FALSE)

# 合并数据便于后续分析
data_all <- dplyr::bind_rows(data_color, data_shape) %>%
  dplyr::mutate(
    task_type = case_when(
      # group = color 时的规则
      group == "color" & part == "RDK_color" ~ "relevant",
      group == "color" & part == "RDK_motion" ~ "irrelevant",
      # group = motion 时的规则
      group == "shape" & part == "RDK_color" ~ "irrelevant",
      group == "shape" & part == "RDK_shape" ~ "relevant",
      # 通用重叠任务
      part == "RDK_overlap" ~ "overlap",
      TRUE ~ NA_character_
    )
  )

# 输出到新的CSV文件
output_file <- base::file.path(output_path, "data_all.csv")
utils::write.csv(data_all, output_file, row.names = FALSE)
```

## 5. 数据预览

```{r}
library(tidyverse)
library(ggrain)
library(cowplot)
library(papaja)
```



```{r}
rdk_task_analysis <- function(data, task_vec = c("RDK_shape","RDK_color","RDK_overlap")){
  
  # 获取数据名，提取分组（shape/color）
  data_name <- deparse(substitute(data))
  split_result <- strsplit(data_name, "_")[[1]]
  group <- ifelse(length(split_result)>=2, split_result[2], "Unknown")
  group <- paste(group, collapse = "_")
  
  # 读取全部辨别任务三类数据
  data_raw <- data %>% filter(part %in% task_vec)
  
  # ---------------------- 统一构建辨别任务分层标签：任务重合在上，任务相关中间，任务无关在下 ----------------------
  data <- data_raw %>%
    dplyr::mutate(
      # 调换levels顺序：other在前，self在后 → 图例他人在上、自我在下
      association = factor(
        association,
        levels = c("other", "self"),
        labels = c("他人", "自我")
      ),
      condition = association,
      # 自动判定任务相关 / 任务重合 / 任务无关
      disc_layer = case_when(
        # shape组：RDK_shape = 任务相关
        group == "shape" & part == "RDK_shape" ~ "任务相关",
        group == "shape" & part == "RDK_color" ~ "任务无关",
        # color组：RDK_color = 任务相关
        group == "color" & part == "RDK_color" ~ "任务相关",
        group == "color" & part == "RDK_shape" ~ "任务无关",
        # 两组通用：RDK_overlap统一为任务重合
        part == "RDK_overlap" ~ "任务重合",
        TRUE ~ NA_character_
      ),
      # 分面垂直显示顺序：任务重合(上) → 任务相关(中) → 任务无关(下)
      disc_layer = factor(disc_layer, levels = c("任务重合", "任务相关", "任务无关"))
    ) %>% drop_na(disc_layer)
  
  # 第一个对应他人，第二个对应自我
  color_palette <- c("#99C2FF", "#0055BB")
  names(color_palette) <- levels(data$condition)
  
  # ---------------------- 辨别任务-ACC图 ----------------------
  acc_plot <- ggplot(
    data,
    aes(x = condition, y = acc, fill = condition, colour = condition, alpha = association)
  ) +
    geom_flat_violin(
      position = position_nudge(x = 0.15, y = 0),
      adjust = 1.2,
      trim = FALSE,
      alpha = 0.7,
      scale = "width",
      flip = TRUE # 雨云倒置
    ) +
    geom_jitter(
      aes(x = as.numeric(condition)),
      width = 0.05,
      alpha = 0.7,
      seed = 123,
      size = 1.2,
      show.legend = FALSE
    ) +
    geom_boxplot(
      outlier.shape = NA,
      width = 0.07,
      colour = "black",
      fill = NA,
      show.legend = FALSE
    ) +
    ylab("ACC") +
    xlab("") +
    coord_flip() +
    scale_y_continuous(breaks = seq(0, 1, by = 0.2), limits = c(0, 1)) +
    scale_fill_manual(values = color_palette) +
    scale_colour_manual(values = color_palette) +
    scale_alpha_manual(
      values = c("他人" = 0.5, "自我" = 1.0),
      name = "关联类型"
    ) +
    theme_cowplot() +
    papaja::theme_apa() +
    guides(fill = "none", colour = "none", alpha = "none") +
    facet_wrap(~disc_layer, ncol = 1, scales = "free_y") +
    theme(
      axis.title.x = element_text(size = 30, margin = margin(t = 8)),
      axis.text.x = element_text(size = 30),
      axis.text.y = element_blank(),
      axis.ticks.y = element_blank(),
      plot.margin = margin(t = 10, r = 5, b = 10, l = 10),
      strip.text = element_text(size = 20, face = "bold"), # 分面标题大小
      strip.background = element_blank()
    )
  
  # ---------------------- 辨别任务-RT图 ----------------------
  rt_plot <- ggplot(
    data,
    aes(x = condition, y = rt, fill = condition, colour = condition, alpha = association)
  ) +
    geom_flat_violin(
      position = position_nudge(x = 0.15, y = 0),
      adjust = 1.2,
      trim = FALSE,
      alpha = 0.7,
      scale = "width",
      flip = TRUE # 雨云倒置
    ) +
    geom_jitter(
      aes(x = as.numeric(condition)),
      width = 0.05,
      alpha = 0.7,
      seed = 123,
      size = 1.2,
      show.legend = FALSE
    ) +
    geom_boxplot(
      outlier.shape = NA,
      width = 0.07,
      colour = "black",
      fill = NA,
      show.legend = FALSE
    ) +
    ylab("RT (ms)") +
    xlab("") +
    coord_flip() +
    scale_y_continuous(breaks = seq(500, 3000, by = 500)) +
    scale_fill_manual(values = color_palette) +
    scale_colour_manual(values = color_palette) +
    scale_alpha_manual(
      values = c("他人" = 0.5, "自我" = 1.0),
      name = "关联类型"
    ) +
    theme_cowplot() +
    papaja::theme_apa() +
    guides(
      fill = guide_legend(order = 1, title = "关联类型"),
      colour = "none",
      alpha = "none"
    ) +
    facet_wrap(~disc_layer, ncol = 1, scales = "free_y") +
    theme(
      axis.title.x = element_text(size = 30, margin = margin(t = 8)),
      axis.text.x = element_text(size = 30),
      axis.text.y = element_text(size = 30),
      axis.ticks.y = element_line(size = 0.5),
      legend.title = element_text(size = 28),
      legend.text = element_text(size = 28),
      legend.key.size = unit(2, "cm"),
      plot.margin = margin(t = 10, r = 10, b = 10, l = 0),
      strip.text = element_text(size = 20, face = "bold"),
      strip.background = element_blank()
    )
  
  # -------------------------- 统一组合布局 --------------------------
  plot_title <- paste0("辨别任务 ", group)
  combined_plot <- rt_plot + acc_plot +
    plot_layout(
      ncol = 2,
      widths = c(1.1, 0.9),
      guides = "collect"
    ) &
    theme(legend.position = "right")
  
  print(combined_plot)
  return(invisible(combined_plot))
}
```

### 辨别任务

```{r}
p <-rdk_task_analysis(data_shape_rt_acc, task_vec = c("RDK_shape","RDK_color","RDK_overlap"))
ggsave(
  filename = file.path(output_path, "motion_rdk_rt_acc.svg"),
  plot = p,
  width = 16,    
  height = 12,    
  dpi = 600,    
  bg = "white"   
)

p <-rdk_task_analysis(data_color_rt_acc, task_vec = c("RDK_motion","RDK_color"))
ggsave(
  filename = file.path(output_path, "color_rdk_rt_acc.svg"),
  plot = p,
  width = 16,   
  height = 12,    
  dpi = 600,     
  bg = "white"  
)
```







